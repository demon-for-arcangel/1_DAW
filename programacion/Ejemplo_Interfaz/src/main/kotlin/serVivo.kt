interface serVivo {
    fun nacer()
    fun crecer() // al poner las llaves estoy implementando la funcion y aunque esten vacias tienen codigo si no ponemos las llaves no tienen codigo
    fun morir()
}