/*
Una sola variable llamada contador que vaya contando del 1 al 20
una vez que evaluamos contador y dependiendo de lo que sea lo meto en el vector pares o vector impares
Con un bucle while
 */

fun main(args: Array<String>) {
    var contador: Int = 0
    var num: Int = 1
    var num_par: Array<Int> = arrayOf()
    var num_impar: Array<Int> = arrayOf()

    while(0 < contador && contador > 20){
        if(){
            contador mod 2 =

        }else{

        }
    }
    contador++
}